import { FC } from 'react';

import { Layout } from '../Layout';

export const App: FC = () => {
  return (
    <Layout>
      <div className="App">App</div>
    </Layout>
  );
};

export default App;
